---
const { type = 'submit', ...props } = Astro.props;
---
<button type={type} {...props}>
  <slot/>
</button>

<style>
  button {
    display: block;
    width: 100%;
    margin-top: 16px;
    font-family: GeistSans, Geneva, sans-serif;
    font-size: 14px;
    font-weight: 500;
    background-color: rgb(0,0,0);
    color: #fff;
    border-radius: 6px;
    cursor: pointer;
    padding: 8px 16px;
  }

  button:hover {
    background-color: rgb(31, 41, 55);
  }

  button:focus {
    outline: 2px solid rgb(0, 0, 0);
    outline-offset: 2px;
  }
</style>
